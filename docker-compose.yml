services:
  backend:
    build: ./backend
    network_mode: "host"
    volumes:
      - ./backend:/app
    environment:
      - PYTHONUNBUFFERED=1

  frontend:
    build: ./frontend
    network_mode: "host"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    depends_on:
      - backend
